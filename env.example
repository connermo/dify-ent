# ==================================================================
# Dify Enterprise SSO Environment Variables
# Copy this file to .env and update the values
# ==================================================================

# GitHub Configuration
GITHUB_USERNAME=your-github-username

# Keycloak OAuth Configuration
KEYCLOAK_CLIENT_ID=dify-console
KEYCLOAK_CLIENT_SECRET=dify-console-secret
KEYCLOAK_ISSUER_URL=http://localhost:8280/realms/dify

# Keycloak Admin Configuration
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# Dify URLs (adjust ports as needed)
CONSOLE_API_URL=http://localhost:5001
CONSOLE_WEB_URL=http://localhost:3000
SERVICE_API_URL=http://localhost:5001
APP_API_URL=http://localhost:5001
APP_WEB_URL=http://localhost:3000

# Security Configuration
SECRET_KEY=your-secret-key-here
INIT_PASSWORD=your-initial-password

# Database Configuration
DB_USERNAME=postgres
DB_PASSWORD=difyai123456
DB_HOST=db
DB_PORT=5432
DB_DATABASE=dify

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=difyai123456
REDIS_DB=0

# Celery Configuration
CELERY_BROKER_URL=redis://:difyai123456@redis:6379/1
CELERY_BACKEND=redis
CELERY_WORKER_AMOUNT=2

# OAuth Configuration
ENABLE_SOCIAL_OAUTH_LOGIN=true

# Other Configuration
DEPLOY_ENV=PRODUCTION
LANG=en_US.UTF-8
LC_ALL=en_US.UTF-8
PYTHONIOENCODING=utf-8
LOG_LEVEL=INFO
DEBUG=false
FLASK_DEBUG=false
NODE_ENV=production

# ==================================================================
# Quick Start Instructions
# ==================================================================
#
# 1. Copy this file to .env:
#    cp env.example .env
#
# 2. Update the following values:
#    - GITHUB_USERNAME: Your GitHub username
#    - SECRET_KEY: Generate a secure random string
#    - INIT_PASSWORD: Set your initial admin password
#
# 3. Start the services:
#    docker compose -f docker-compose.example.yml up -d
#
# 4. Access the services:
#    - Dify Console: http://localhost:3000
#    - Dify API: http://localhost:5001
#    - Keycloak Admin: http://localhost:8280/admin
#
# 5. Login to Keycloak with admin/admin
# 6. Create a new realm called "dify"
# 7. Create a new client with the credentials above
# 8. Login to Dify with Keycloak SSO
#
# ==================================================================
